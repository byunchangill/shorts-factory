# 다음에 할 일

마지막 작업: 2026-08-24 (집 PC) · 브랜치 `feat/menu-b-video-and-assets`

---

## 1. ~~하네스가 menu-b를 안 돈다~~ ✅ 2026-08-24

붙였다. menu-b 조립 시나리오 3단계 — 소재 3건 첨부 → 대본·승인·음성(첨부) → 조립 검사.
하네스 **47단계 전부 통과**(464초). 실측: `4씬 → 12컷(소재 2종 · 컷 평균 1.67초) ·
계획 20.09초 = 나레이션 20.09초 · 출력 20.0초`.

- **컷 계획을 `assemble.done` 감사 이벤트에 남기게 했다.** 컷은 `output/tmp/`에서 렌더된 뒤
  지워져 결과물에 아무 신호도 안 남았다. 씬별 컷 수·소재 수·컷 길이 합이 남는다
- **OCR 의존을 없앴다** — 존을 하네스가 직접 심어(앞 둘 비움 · 세 번째만 화면 한가운데)
  정답을 「씬마다 소재 2개」로 고정했다. 한 번의 렌더로 `hasVisibleText` 양쪽 분기가 다 덮인다
- 🔴 **단언은 결과물이 아니라 계획을 잰다.** 최종 먹싱이 `-shortest`라 컷 합이 **길어지면**
  영상 뒤가 조용히 잘려 출력 길이가 정확히 나레이션 길이가 된다 — 첫 판은 컷을 15% 늘려도
  안 터졌다. 계획을 재는 단언(②-a)을 따로 뒀다

### ~~남은 별건 — 조립에 불변식 검사~~ ✅ 2026-08-24

`cutPlanError`(assemble.ts)가 씬마다 컷 계획을 세운 직후 「컷 합 ≠ 나레이션」이면
**한 프레임도 인코딩하기 전에** 막는다. 훅 게이트와 같은 자리·같은 논리다.

- 오차 `CUT_SUM_TOLERANCE_SEC`(0.05초)는 `shared/constants.ts` 한 곳에서 나온다 —
  조립·하네스·단위 테스트가 같은 값을 쓴다
- **정상 입력으로는 절대 안 걸리게 만든 값이라 하네스로는 못 흔든다.**
  양방향 검사는 `cuts.test.ts`의 `cutPlanError`가 맡는다 (planCuts가 내놓는 모든 모양이
  통과하는지까지 전수로 본다)
- 하네스 ②-a는 남겼다 — 이제 **감사 로그 ↔ 타이밍 파일**의 짝을 보는 다른 층이다

## 2. 인트로 타이틀 카드 (벤치마킹에서 가져올 세 번째 항목)

레퍼런스 3편이 전부 **1.8~2.2초짜리 제목 카드**로 연다. 문구는 상단 띠 제목과 같고
두 톤(노랑/흰색)으로 나뉜다.

🔴 **정지 이미지로 만들면 안 된다.** 우리 훅 게이트로 레퍼런스를 채점해 보니
쇼핑 채널 셋은 10.6·18.4·22.2로 통과했다 — **움직이는 제품 영상 위에 큰 제목을 얹은 것**이지
정지 카드가 아니다. 정지 카드로 만들면 우리가 14편으로 잰 유일한 유효 변수(첫 0.5초
화면 변화량, r=+0.57)와 정면으로 부딪힌다.

**할 일:** 첫 컷 위에 2초짜리 큰 제목 오버레이. `splitHeadline`·`fitTitleSize`가 이미 있으니
그 문구를 키워 `drawtext`로 얹으면 된다.

---

## 벤치마킹에서 **안** 가져오기로 한 것 (2026-08-23 판단)

되살리자는 이야기가 나오면 이 이유부터 다시 볼 것.

- **잘한줌식 이미지 포맷** (짤·스크린샷만으로 만드는 정보/썰 포맷) — 매력적이지만
  우리 훅 게이트에서 **7.0과 2.0으로 떨어진다.** 하려면 별도 채널로, 게이트를 끄고
  성적을 따로 재야 한다
- **자막 바를 영상 밖으로 빼기** — 근거가 모방뿐인데 공사는 제일 크다(레이아웃·자막·존
  계산이 다 물린다). 1·2번 효과를 보고 판단해도 늦지 않다
- **가짜 조회수·댓글 수** — 세 채널 다 쓰지만 지어낸 숫자다(핫템리포트는 두 편이
  369,451로 동일). 시청자에게는 실제 수치로 읽히므로 안 넣는다. 날짜만으로도
  뉴스 UI 질감은 나온다

## 확인·정리할 것

- **낭독 배속 1.6은 표본 하나로 정한 값이다.** 다음 편들에서 실제 길이를 재서
  `SYLLABLES_PER_MIN = 245`가 맞는지 다시 본다 (2026-08-23 실측: 1.33→5.23 ·
  1.5→6.08 · 1.6→6.51 · 1.7→6.56 음절/초. 1.6 위로는 앞뒤 무음 때문에 거의 안 빨라진다)
- **카테고리 `생활용품`의 `guidelines/script.md`가 옛 규칙(템캐스팅 6비트)이다.**
  `MENU_SKILL`을 바꿔도 **이미 만든 카테고리는 자동으로 안 바뀐다** — 화면에서
  붙여넣거나 파일을 갈아야 한다. 안 그러면 고쳐놓고 옛 규칙으로 계속 뽑는다
- **옛 스킬 둘(`temcasting-shorts`·`shorts-viral-script`)이 1.25배속·글자 단위로 남아 있다.**
  이번 배속 변경 전에도 이미 앱과 어긋나 있었고, 글자 기반이라 절반만 고치면 더 나빠진다.
  쓸 일이 생기면 통째로 음절 기준으로 옮긴다
- **`생각중-소년-워터마크있음.jpg`에 dreamstime 워터마크가 있다.** 지울지 교체할지 결정
- **마동석 짤에 「REPORT」 워터마크**가 오른쪽 아래에 보인다 (마지막 씬에 쓰인다)

## 회사 PC에서 이어받을 때

```bash
git fetch origin
git checkout feat/menu-b-video-and-assets
npm install
npm run dev
```

- **`workspace/`는 깃에 없다.** 회사 PC에는 카테고리·잡·API 키가 비어 있다.
  화면을 채우려면 `npm run seed` (샘플 잡 하나를 영상 분석 단계까지 만든다)
- **API 키는 `workspace/secrets.json`에만 있다** — 타입캐스트·유튜브 등은 다시 넣어야 한다
- 도구 점검: `npm run doctor` (ffmpeg·yt-dlp·폰트·검출기 경로를 그대로 보여준다)
- 단위 테스트 `npm test` · E2E `npm run harness`
  (🔴 하네스는 포트 4310을 쓴다 — `npm run dev`를 끄고 돌리고, **끝나면 다시 켠다**)
