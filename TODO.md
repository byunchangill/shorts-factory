# 다음에 할 일

마지막 작업: 2026-08-26 · PR #46 머지

## 🚧 지금 잡고 있는 것 (2026-08-26 · 로컬)

**`feat/asset-source-policy`** — 에셋 소싱 정책 + 출처 대장 (menu-b).
`shared/assetPolicy.ts`(정책 단일 출처) · `store/assets.ts`(출처 5필드) ·
`pipeline/assemble.ts`(`assetLogError` 게이트) · `exporter.ts`(에셋출처.csv) ·
`.claude/skills/ssul-shopping`(소싱 블록)을 건드린다.

**클라우드 세션은 이 항목을 잡지 말 것.** 겹치면 PR #43처럼 통째로 충돌한다.

---

## 1. 캡컷 재료의 자막 시각은 아직 `timing.duration` 기준이다 🔴 먼저

`capcut.ts:87`이 `timing?.duration`으로 자막 큐를 잡는다 — **첨부한 mp3의 컨테이너
길이**이고, 조립이 2026-08-24에 「믿지 말자」고 결론 낸 바로 그 값이다
(실측 2.000초 소리가 2.064초로 잡힌다).

⚠️ **버그로 단정하지 말 것.** 캡컷에 끌어다 놓는 것은 **원본 mp3 그대로**라, 캡컷이
그 파일을 컨테이너 길이로 읽으면 오히려 맞는다. 디코딩 길이로 읽으면 씬마다 어긋난다.

**할 일:** 캡컷에서 실제로 확인한다 — 재료 묶음을 받아 넣고, 뒤쪽 씬에서 자막이
음성과 밀리는지 눈으로 본다. 밀리면 조립과 같은 방식(규격 맞춘 뒤 재기)으로 옮긴다.

---

## 1-1. 씬 이미지(`imageRef`)에는 출처를 못 적는다 🔴 에셋 정책의 남은 구멍

에셋 소싱 정책(2026-08-26)은 **짤방·효과음만** 본다. 소싱 위험이 실제로 큰 자리는
**씬 이미지**인데 — 스톡 인물 사진이 들어올 자리가 정확히 거기다 — `SceneLine.imageRef`가
**문자열 하나**라 출처를 붙일 자리가 없다.

조립 게이트의 안내문이 이 사실을 말하고 있다(「씬 이미지는 아직 출처를 기록하지
않습니다」) — 안 그러면 「이미지 출처를 대체 어디 적으라는 거냐」에서 막힌다.

**할 일:** `imageRef`를 객체로 올리는 스키마 마이그레이션. 기존 잡의 문자열을 읽어야
하므로 `z.union([z.string(), ImageRefSchema])`로 받아 읽을 때 정규화한다.
그 뒤 `usedAssetIds`가 씬 이미지까지 걷으면 게이트·대장이 그대로 따라온다.

---

## 1-2. 기존 편집 재료 60개에 출처가 없다 🔴 운영에 바로 걸린다

에셋 소싱 정책(2026-08-26)이 들어오면서 **이 PC의 자료 60개(짤방 30 · 효과음 30)가
전부 「출처 확인 필요」**가 됐다. 목록·캡컷 묶음은 그대로 되지만 **그 자료를 담은
menu-b 잡은 웹 조립이 막힌다.** 정책상 의도한 동작이다.

- 자료실을 열었을 때 전부 노란 배지면 사용자는 정책이 아니라 **앱이 고장 났다**고 읽는다
- 커뮤니티에서 모은 짤은 블랙리스트라 **URL을 적어도 안 풀린다** — 교체가 답이다
- 급한 것부터: 다음 편에 실제로 쓸 자료만 출처를 채우면 된다. 전수 정리는 나중에

**할 일:** 쓸 자료부터 출처를 채우고, 못 채우는 것은 무료 소재 사이트 자료로 바꾼다.
화면에 「출처 없는 자료 N개」 안내 배너를 띄울지는 별도 판단.

---

## 확인·정리할 것

- **낭독 배속 1.6은 표본 하나로 정한 값이다.** 다음 편들에서 실제 길이를 재서
  `SYLLABLES_PER_MIN = 245`가 맞는지 다시 본다 (2026-08-23 실측: 1.33→5.23 ·
  1.5→6.08 · 1.6→6.51 · 1.7→6.56 음절/초. 1.6 위로는 앞뒤 무음 때문에 거의 안 빨라진다)
- **이 PC의 카테고리 지침은 2026-08-26에 전부 맞췄다.** `MENU_SKILL`을 바꿔도
  **이미 만든 카테고리는 자동으로 안 바뀐다** — 화면에서 붙여넣거나 파일을 갈아야 한다.
  🔴 **`workspace/`는 깃에 없어서 회사 PC·클라우드에는 이 수정이 안 간다.** 다른 PC에서
  옛 지침으로 뽑고 있는지 먼저 본다 (menu-b는 첫 줄이 `# 썰형 쇼핑 대본 교리 v1`이어야 한다)
- **옛 스킬 둘(`temcasting-shorts`·`shorts-viral-script`)이 1.25배속·글자 단위로 남아 있다.**
  이번 배속 변경 전에도 이미 앱과 어긋나 있었고, 글자 기반이라 절반만 고치면 더 나빠진다.
  쓸 일이 생기면 통째로 음절 기준으로 옮긴다
- **내보내기 CSV의 `변형` 칸은 「지금 설정」으로 다시 계산된다.** 조립 뒤에 좌우반전을
  껐다 켜면 그 편의 대장이 실제와 어긋난다 (감사 이벤트 `assemble.done`의 대장은 조립
  시점 값이라 맞다). 고친다면 내보내기가 마지막 `assemble.done`의 대장을 그대로 쓰는 쪽이다
- **자료의 출처 5필드가 일곱 군데에 따로 열거돼 있다** — `types.ts:ASSET_SOURCE_FIELDS` ·
  `assetPolicy.ts:AssetSource` · `store/assets.ts`의 `sourceOf`·`mergeSource`·`text()` 키 유니온 ·
  `routes/assets.ts:metaBody` · `AssetsPage:SourceDraft`. 🔴 **여섯째 필드를 더할 때 하나를
  빠뜨려도 구조적 타이핑이라 컴파일이 통과한다** — 그 필드만 조용히 안 실린다.
  고친다면 `AssetSource`를 `Pick<Asset, …>`로 파생시키고 `sourceOf`/`mergeSource`가
  `SOURCE_KEYS` 상수를 도는 형태로. 스키마·store·라우트·화면을 한꺼번에 건드리는 정리라
  기능 변경과 같은 PR에 섞지 않는다
- **`setAssetMeta`의 읽기-수정-쓰기에는 파일 락이 없다.** 지금은 자료실 업로드가 바깥에서
  `local.json` 키로 잡아 주지만, `PATCH /assets/:id` 단독 경로는 락 없이 돈다.
  🔴 **고치려면 바깥 락을 같이 빼야 한다** — `withFileLock`은 재진입이 안 돼서 같은 키를
  겹쳐 잡으면 영원히 안 끝난다 (경고는 `store/assets.ts`의 `setAssetMeta` 독스트링과
  CLAUDE.md 「코드 규칙」에 있다). 반쪽만 건드리면 모든 업로드가 조용히 멈춘다
- **`생각중-소년-워터마크있음.jpg`에 dreamstime 워터마크가 있다.** 지울지 교체할지 결정
- **마동석 짤에 「REPORT」 워터마크**가 오른쪽 아래에 보인다 (마지막 씬에 쓰인다)
- **인트로 타이틀은 `banded`에서만 띠 제목을 물린다.** `fullscreen`은 띠가 없어 물릴
  것도 없고, `framed`의 상단 문구는 채널명이라 안 물린다 — 지금은 맞는 동작이지만,
  menu-b에서 `banded` 말고 다른 레이아웃을 쓰기 시작하면 다시 볼 자리다
- **하네스의 픽셀 검사는 한글 폰트가 있어야 뜻이 있다.** 폰트가 없으면 인트로 제목도
  띠 제목도 안 그려지는데(`fontArg` 없으면 건너뛴다), 띠 표준편차 검사는 그걸 「제목이
  안 돌아왔다」로 읽는다. 지금 CI 러너에는 폰트가 있어 안 걸린다 — 러너를 바꾸면 볼 자리다

## 벤치마킹에서 **안** 가져오기로 한 것 (2026-08-23 판단)

되살리자는 이야기가 나오면 이 이유부터 다시 볼 것.

- **잘한줌식 이미지 포맷** (짤·스크린샷만으로 만드는 정보/썰 포맷) — 매력적이지만
  우리 훅 게이트에서 **7.0과 2.0으로 떨어진다.** 하려면 별도 채널로, 게이트를 끄고
  성적을 따로 재야 한다
- **자막 바를 영상 밖으로 빼기** — 근거가 모방뿐인데 공사는 제일 크다(레이아웃·자막·존
  계산이 다 물린다). 컷 쪼개기·인트로 타이틀 효과를 보고 판단해도 늦지 않다
- **가짜 조회수·댓글 수** — 세 채널 다 쓰지만 지어낸 숫자다(핫템리포트는 두 편이
  369,451로 동일). 시청자에게는 실제 수치로 읽히므로 안 넣는다. 날짜만으로도
  뉴스 UI 질감은 나온다

---

## 끝난 것

### 2026-08-25 · PR #43 — 컷 계획 불변식 · 하네스 합치기 · 프레임 정렬

| 무엇 | 어디 |
|---|---|
| `cutPlanError` — 컷 합 ≠ 나레이션이면 렌더 전에 막는다 | 「컷 합 ≠ 나레이션이면 렌더 전에 막는다」 |
| menu-b 조립 하네스 두 벌을 한 판으로 합침 | `tools/harness.ts` 37·38단계 |
| `planCuts`를 프레임 경계로 나눈다 | 「컷 길이는 프레임 경계에 맞춰 나눈다」 |

🔴 **합친 하네스가 곧바로 `main`의 결함을 잡았다.** 씬 길이를 3·5·7초로 갈라 놓자
첫 씬에서 조립이 멈췄다 — `total / n`으로 나눈 컷은 프레임 경계에 안 걸려 조각마다
깎이고, 나레이션 3.022초를 둘로 나눈 1.511초가 **각각 1.500초**로 나왔다.
`sceneLengthError`가 「소재가 모자라다」며 세웠는데 **소재는 8초짜리였다.**
씬 길이가 상한의 정수 배가 아닐 때만 드러나서 그동안 안 보였다.

**여기서 얻은 것 —** 고정물을 **비대칭으로** 만들어야 잡힌다. 씬 길이가 전부 같으면
감사 로그가 씬 순서와 어긋나도 값이 안 변해 짝 검사가 조용히 죽고, 컷 길이가 상한의
정수 배면 프레임 깎임이 안 드러난다. 같은 값을 반복하는 고정물은 아무것도 안 지킨다.

### 2026-08-25 · PR #40~#42

| 무엇 | 어디 |
|---|---|
| 조립 길이 불변식 — `-shortest`가 가리던 0.6초 밀림 | 「길이는 재서 맞춘다」 |
| 인트로 타이틀 오버레이 (정지 카드 아님, menu-b 전용) | 「인트로 타이틀은 카드가 아니라 오버레이다」 |
| 하네스가 menu-b를 조립까지 — 픽셀로 검사 | 「하네스는 제품정보리뷰도 조립까지 돈다」 |
| 텍스트 카드 E2E — 늘어난 만큼만 길어진다 | `tools/harness.ts` 38단계 |
| `tools/`도 타입체크 (`npm run build`) | 「코드 규칙」 |

**여기서 얻은 습관 하나 —** 게이트를 만들면 **해당 기능을 꺼놓고 돌려** 실제로
빨개지는지 본다. 이번에 컷 쪼개기 검사가 처음엔 무력했던 것을 그렇게 잡았다
(꺼놓고 돌렸는데 그냥 통과했다).

## 회사 PC에서 이어받을 때

```bash
git fetch origin
git checkout main && git pull
npm install
npm run dev
```

- **`workspace/`는 깃에 없다.** 회사 PC에는 카테고리·잡·API 키가 비어 있다.
  화면을 채우려면 `npm run seed` (샘플 잡 하나를 영상 분석 단계까지 만든다)
- **API 키는 `workspace/secrets.json`에만 있다** — 타입캐스트·유튜브 등은 다시 넣어야 한다
- 도구 점검: `npm run doctor` (ffmpeg·yt-dlp·폰트·검출기 경로를 그대로 보여준다)
- 빌드·타입체크 `npm run build` (server·client·**tools** 셋 다) · 단위 테스트 `npm test`
- E2E `npm run harness`
  (🔴 포트 4310·4399를 쓴다 — `npm run dev`를 끄고 돌리고, **끝나면 다시 켠다**.
  실측 로컬 439초 · CI 154초. 이전 판이 죽다 남으면 포트를 물고 있어
  `EADDRINUSE`로 안 뜬다 — 남은 node 프로세스를 먼저 정리한다)

## 🔴 클라우드 세션과 같은 일을 잡지 않기

PR #43에서 **로컬 브랜치와 클라우드 브랜치가 같은 항목**(「하네스가 menu-b를 조립까지」)을
각자 만들어 놔서 `tools/harness.ts`가 500줄 통째로 충돌했다. 둘 다 멀쩡한 코드였고
합치는 데만 한나절이 갔다.

**시작하기 전에 이 파일에 「지금 누가 무엇을 잡고 있다」를 적는다.** 클라우드 세션은
로컬 작업 트리를 못 보고, 로컬은 클라우드가 뭘 하는지 커밋이 올라오기 전엔 모른다 —
이 파일이 둘 사이의 유일한 접점이다.
