# 의존성
node_modules/
# AI 인페인팅용 파이썬 환경 (기계마다 따로 — tools/install-inpaint.md)
.venv-inpaint/

# 빌드
dist/
*.tsbuildinfo

# 사용자 작업 데이터 (템플릿만 커밋)
workspace/*
!workspace/templates/
!workspace/settings.example.json

# OS
.DS_Store
Thumbs.db

# 로그
*.log

# 로컬 Claude Code 설정
.claude/settings.local.json

# 에이전트 팀 중간 산출물
_workspace/
_workspace_prev/

# 설치본이 만드는 파일 (개발 클론에는 없다)
/.version
/tools/win/local-config.cmd
